let formattedHour = String(hour).padStart(2, '0');
        // let formattedMinutes = String(date.getMinutes()).padStart(2, '0');
        // let formattedSeconds = String(date.getSeconds()).padStart(2, '0');